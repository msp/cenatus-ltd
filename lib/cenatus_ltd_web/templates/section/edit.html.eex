<h2>Edit section</h2>

<%= render "form.html", changeset: @changeset,
                        action: Routes.section_path(@conn, :update, @section) %>

<%= link "Back", to: Routes.section_path(@conn, :index) %>
