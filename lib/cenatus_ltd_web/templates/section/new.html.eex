<h2>New section</h2>

<%= render "form.html", changeset: @changeset,
                        action: Routes.section_path(@conn, :create) %>

<%= link "Back", to: Routes.section_path(@conn, :index) %>
