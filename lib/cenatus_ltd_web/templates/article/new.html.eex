<h2>New article</h2>

<%= render "form.html", changeset: @changeset,
                        sections: @sections,
                        categories: @categories,
                        tags: @tags,
                        tech_tags: @tech_tags,
                        action: Routes.article_path(@conn, :create) %>

<%= link "Back", to: Routes.article_path(@conn, :index) %>
